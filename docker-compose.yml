version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: test_project_web
    ports:
      - "1111:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
